=form_for Photo.new, url: photos_path, :html => { :multipart => true } do |form|
  -if @photo.count < 5
    =form.file_field :image
    =form.submit
- if (@photo.count) > 4
  = link_to 'Slider', photos_path, class: 'navbar-brand'
